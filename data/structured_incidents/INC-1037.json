{
  "incident_id": "INC-1037",
  "service": "order-service",
  "symptoms": [
    "Primary DB CPU jumped from 18% to 99% within 12 minutes of deploy",
    "Slow query log: 80 000 individual SELECT on products table per minute",
    "GET /orders/history p99 latency: 140 ms → 9 200 ms",
    "Read replica replication lag: 52 seconds behind primary",
    "DB connection pool at 97% — downstream services starting to queue"
  ],
  "hypotheses_tested": [
    "recent deploy caused the issue"
  ],
  "failed_attempts": [],
  "confirmed_root_cause": "ProductService.get_by_id() is called once per order item in a Python for-loop, issuing one SELECT per call",
  "resolution_action": "Hotfix: replaced loop with single SELECT ... WHERE id IN (all_item_ids) batch query. Deployed behind feature flag, validated 99.8% query reduction in staging. DB CPU back to 16% within 3 minutes of hotfix. Added query count assertion to integration test: assert mock_db.call_count == 1.",
  "services_affected": [
    "order-service",
    "user-service",
    "inventory-service"
  ],
  "confidence": 0.98
}